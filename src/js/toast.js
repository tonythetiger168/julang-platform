/* ===== 全局 Toast 通知 ===== */// @ts-checkfunction showToast(message, duration = 2200) {  let t = document.getElementById('global-toast');  if (!t) {    t = document.createElement('div');    t.id = 'global-toast';    t.className = 'fixed top-4 left-1/2 -translate-x-1/2 z-[100] px-4 py-2 rounded-xl bg-black/80 text-white text-sm backdrop-blur transition-opacity duration-300 pointer-events-none';    document.body.appendChild(t);  }  t.textContent = message;  t.style.opacity = '1';  clearTimeout(t._timer);  t._timer = setTimeout(() => { t.style.opacity = '0'; }, duration);}window.showToast = showToast;