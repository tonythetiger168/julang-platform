const userService = require('../services/userService'); const { success, error } = require('../utils/response'); async function getProfile(req, res) { try { const data = await userService.getProfile(req.user.userId); success(res, data); } catch (e) { error(res, 404, e.message); } } async function getCoins(req, res) { try { const data = await userService.getCoins(req.user.userId); success(res, data); } catch (e) { error(res, 500, '獲取金幣失敗'); } } async function checkin(req, res) { try { const data = await userService.checkin(req.user.userId); success(res, data, '簽到成功'); } catch (e) { error(res, 400, e.message); } } async function getFollows(req, res) { try { const data = await userService.getFollows(req.user.userId); success(res, data); } catch (e) { console.error(e); error(res, 500, '獲取追劇列表失敗'); } } async function getHistory(req, res) { try { const data = await userService.getHistory(req.user.userId); success(res, data); } catch (e) { console.error(e); error(res, 500, '獲取歷史失敗'); } } module.exports = { getProfile, getCoins, checkin, getFollows, getHistory };