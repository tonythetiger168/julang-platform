const redis = require('../config/redis'); function cache(keyPrefix, ttlSeconds = 60) { return async (req, res, next) => { const key = `${keyPrefix}:${JSON.stringify(req.query)}:${req.params.id || ''}`; try { const cached = await redis.get(key); if (cached) { return res.json(JSON.parse(cached)); } res.sendJson = res.json; res.json = (data) => { redis.setEx(key, ttlSeconds, JSON.stringify(data)); res.sendJson(data); }; next(); } catch { next(); } }; } module.exports = cache;