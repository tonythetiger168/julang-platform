const express = require('express');const router = express.Router();const auth = require('../middleware/auth');const assetController = require('../controllers/assetController');router.get('/', auth, assetController.list);router.post('/', auth, assetController.uploadMiddleware, assetController.create);router.post('/sync', auth, assetController.sync);router.delete('/:id', auth, assetController.remove);module.exports = router;