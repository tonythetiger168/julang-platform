const express = require('express');const router = express.Router();const auth = require('../middleware/auth');const ctrl = require('../controllers/subscriptionController');router.get('/', auth, ctrl.getSubscription);router.get('/plans', ctrl.getPlans);router.post('/upgrade', auth, ctrl.upgrade);router.post('/downgrade', auth, ctrl.downgrade);router.post('/cancel', auth, ctrl.cancelRenewal);module.exports = router;