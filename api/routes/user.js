const express = require('express'); const router = express.Router(); const userController = require('../controllers/userController'); const auth = require('../middleware/auth'); router.get('/profile', auth, userController.getProfile); router.get('/coins', auth, userController.getCoins); router.post('/checkin', auth, userController.checkin); router.get('/follows', auth, userController.getFollows); router.get('/history', auth, userController.getHistory); module.exports = router;